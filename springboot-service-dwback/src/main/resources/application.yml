server:
  port: 8002
spring:
  application:
    name: service-dwback
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka